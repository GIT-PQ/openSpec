# 数据集与预处理

在基于深度学习的专利分类任务中，数据质量直接决定了模型的上限。本章旨在通过数据清洗、数据标注和数据增强生成一份高质量、覆盖22个一级产品类别的医疗器械发明专利训练数据集。

首先，详细介绍数据的来源、检索策略及严格地纳入标准，明确原始数据集的构成。其次，阐述数据清洗的具体规则，通过去除噪声和规范化处理提升文本的语义表达质量。第三，针对原始数据标签质量低的难题，设计基于自适应分层抽样与JS散度评估的策略，构建专家标注子集作为验证标准。第四，论述基于RAG与LLM的自动标注工作流的搭建与优化，解决全量数据的标签补全问题。 最后，针对极少数样本类别实施生成式数据增强，提升训练数据集的质量，为后续模型训练奠定基础。

## 数据集介绍

本研究的数据来源于智慧芽专利数据库[62]。为了聚焦于医疗器械领域的发明专利，本文制定了明确的检索策略。检索式设定为：APD:[* TO 20231231] AND MIPC:(A61B NOT A61B16)。


> **图3-1  2024年IPC分类表A部节选**

这个检索式是用来选择申请日期（APD）在2023年12月31日前的所有专利，并且其主要分类号（MIPC）是属于IPC分类表A部下的A61B小类，在2024年的IPC分类表中只有A61B有医疗器械相关的发明专利，具体IPC号及其含义见图3-1。 包括产品和方法。

为保证数据可用以及完整，本文规定原始数据集选取条件为：

已授权

保留法律状态为授权的有效专利，保证技术方案的可行性及专利文本质量。

有附图

保留包含说明书附图的专利，在本文以文本为主的情况下，附图为多模态扩展的基础，对后续工作有益。

存在一级产品类别标签

在从智慧芽专利平台收集数据后，已经用于日常的研究。因此，部分数据存在《医疗器械分类目录》的一级产品类别标签，但存在标签质量较低的问题。

文本未缺失

针对文本的分类研究，文本存在缺失时，是无法纳入的，因为它无法通过插值等手段来填充。在智慧芽专利平台中，缺失文本一般用“-”占位，本文对包含该占位符的数据进行了过滤。

经过上述标准筛选及初步清洗后，原始数据集共包含58495条专利数据。经过后续的数据增强，最终用于模型训练的数据集规模达到58587条，数据的分布情况如表3-1所示。

> **表3-1  一级产品类别及数据**

## 数据清洗

原始专利文本一般带有HTML标签、非标准的特殊字符以及标点符号等噪声信息，这会影响模型对于其语义的理解。因此，在此基础上本文对原始数据中的摘要部分进行规范化操作。即：

去除HTML标签

专利数据采集或者存储时往往带有网页渲染相关HTML标签，例如<br>、<p>等，这些都并非自然语言文本内容，在送入模型之前应当将其去除，否则会作为普通符号被编码，造成无效token比例提升，不利于模型关注到实际语义信息，在预处理阶段去除所有的HTML标签只保留纯文本部分。

去除不可见字符

原始文本中可能包含制表符、回车符以外的控制字符。这些字符虽然眼睛看不见，但是会造成分词错误或者导致文本长度计算有误，在处理时本文尽量保留必要的换行符以便保持文章大致结构，而去除其他不可见控制字符，使得文本编码统一、可控。

标点统一

因为数据来源多样化，在专利文本当中经常出现中英文标点混用的情况。同样的一个语义结构因为使用不同的标点而被当作不同的模式给模型学习造成困扰，为了降低由这种无关的形式所带来的影响，本文把所有的英文字母标点都替换成了相应的中文全角标点，使文本格式更加统一并且有利于模型更好地学习句子之间的语法关系。

空格处理

在某些专利文本中，有较多连续空格或者多余的空白符，在一定程度上会使得文本过长，也会影响模型运行速度。本文对连续空格进行合并操作以缩短文本表示，在不影响文章内容的情况下删去不必要的字符，以便加快之后训练以及预测的速度。

## 专家标注数据集构建

因为原始数据缺少对《医疗器械分类目录》的优质标签，需要在数据集中中采样，获得一个由人工标注的小样本数据集用于评估自动标注流水线效果。

### 二级标签获取

数据集仅有产品的第一级产品类别标签而无第二级产品类别标签，为了实现准确的二层分层抽样，在此之前需要先获得这些数据的第二级产品类标签。因此本文用《医疗器械分类目录》训练多个分类器，对数据集的摘要进行预测从而大致得到它们对应的第二级产品类标签。

本文所使用《医疗器械分类目录》来源于中国食品药品检定研究院公开数据[62]，网站的查询接口参数为“20240618”，预计是2024年6月18日版本，共有206个二级产品类别，1701条记录。

目录中单条数据的字段构成如表3-2所示。为实现语义与摘要的对齐，本文将“三级序号/产品类别”、“产品描述”和“预期用途”字段进行了拼接，格式为：“三级序号/产品类别：产品描述预期用途”。以表中数据为例，拼接后的文本为：“超声手术设备：通常由超声波发生器和带有外科尖端的手持部件组成，手持部件通常由一个换能器、一个连接构件和一个治疗头尖端组成。用于软组织的切割、止血、整形。”。

> **表3-2  《医疗器械分类目录》的字段和值**

基于《医疗器械分类目录》中每个一级产品类别的数据，本文分别训练了 LR、KNN、RF、XGBoost和MLP五种模型，具体流程如图3-2所示。


> **图3-2  二级产品类别分类模型**

首先，使用bge-large-zh-v1.5[63]模型对文本进行嵌入，移除L2归一化。其次，对数据进行上采样以解决部分二级类别数据稀疏问题，并按6:2:2的比例划分训练集、验证集和测试集。在预处理阶段，LR、KNN和MLP模型训练前对嵌入向量进行了L2归一化，而RF与XGBoost作为基于决策树的模型，其划分依赖于单一特征维度的阈值比较，不涉及向量距离或模长，且归一化可能破坏特征间的独立性，因此未进行L2归一化处理。最后，通过网格搜索在验证集上确定最优超参数，并在测试集上评估模型性能，并使用模型对数据集的摘要进行预测，得到二级产品类别。

模型具体的结果如表3-3所示，代表模型在数据集上预测出的二级产品类别数量，代表模型在每个一级产品类别的测试集中Precision的最小值。这两个指标分别从不同侧面反映了模型对数据分布和类别边界的刻画能力。

> **表3-3  模型结果**

衡量的是模型对于二级类别的覆盖率，在进行二级分层抽样的情况下，如果模型可以准确识别更多的二级产品类别，则说明该模型对于各类别有较好的区分能力，有利于防止抽样过程中出现一些比较稀少或者样本数量较少的二级类产品被忽略，进而提高抽样结果在类别的完整性和代表性，KNN在数据集上能识别所有的二级产品，因此它在类别的覆盖率上有较大优势。

而衡量的是以最差类别性能来评价模型的精确率，在最小Precision越高的情况下意味着模型对所有的一级类别的二级类别的预测错误被有效控制，特别是在难以区分或者界限不明的类别中仍然具有较好的识别能力，这有利于降低采样过程中由于系统性误判带来的标签噪声，而MLP获得最高的Value，也即它在一级类别的预测更均匀。

和分别从类别覆盖性和预测可靠性两个方面对二级分层抽样有影响，均可使得得到更优更具有代表性的标注样本集，具体的抽样效果可见3.3.3节。

### 抽样设计

为了使子集能较好反映整个数据集的分布特征，本文对数据集进行了一级类目分层、二级类目自适应抽取的方法进行抽样。对于每一个一级类目中的数据子集，本文提出了一个既有基本比例又有最低限度抽取以及总体数量保证补充的自适应方法来兼顾各个小类目的代表性的同时保证总的样本数量足够大。

基础比例与最小阈值抽样

第一个步骤是遍历每个一级类别下面的所有二级类别。由于长尾类别在固定比例下采样的时候容易导致此类别样本数量不足，因此本文设置了一个最小样本数。对于每个二级类别，令其总样本数目为，那么就有如下步骤：如果，那么这个类别的所有样本都会被选取，尽可能多地保留这些稀有类别信息；否则当时，根据比例计算出需要采样的样本数量，最后选取的数量为，这样可以保证主要类别的比例被放大，但是长尾类别的曝光度最小。

总量保底填充

基础抽样结束后，程序判断已抽取样本总数是否小于所需的最小数量。若，则执行填充操作。但是为了保持原有的第二级类别的比例关系，在填充时并不是简单地从剩下的所有样本中随机选取，而是根据剩余未抽取的数据中各个第二级类别的比例来决定各自要补充的数量。即求出还需要补充的数量，然后按照剩余数据中的各类别所占的比例为其分配相应的补充数量，最后将补充样本与基础样本合并得到最终的抽样结果。该抽样算法的伪代码见算法3-1。




> **算法3-1  抽样算法伪代码**

综合考虑专家标注成本与评估结果的统计可靠性，本文将抽样算法中的参数设置为、和，最终获得的子集数据总量为1104条。

### 抽样结果评估

为定量评估抽样结果的分布一致性，本研究采用JS散度来衡量子集和数据集在类内相似度上的差异。

本文引入类内相似度指标定量刻画不同类别样本在语义空间中的聚集程度.设数据集中共有个类别，第类样本的集合记为，其中 表示属于第类的第个样本的归一化语义向量，为该类别的样本数量。对于任一样本，其相对于任意类别的平均相似度定义为该样本向量与类别 中所有样本向量点积的均值，即：


其中，“”表示向量点积运算，表示集合的势，即样本数量。基于此定义，可得出类内相似度指标，当时，衡量了样本与同类样本在语义空间中的平均紧密度。

具体而言，首先利用预训练文本嵌入模型bge-large-zh-v1.5将专利文本映射为高维语义向量表示，并对所有样本向量进行L2归一化处理，以消除向量模长差异，使向量间的点积等价于余弦相似度。

为了比较不同抽样子集与原始全集在类内相似度分布上的一致性，本文采用Jense-Shannon散度作为分布差异度量指标。JS散度是一种基于Kullback-Leibler散度的对称且有界的分布距离度量，具有良好的数值稳定性，适合用于经验分布之间的比较。JS散度取值范围取决于对数底数，本文采用以2为底的对数计算JS散度，JS散度取值范围为[0,1]，其值越小，表示两个分布越相似，即抽样数据在语义结构层面越好地保持了原始数据的分布特征。

本文采用JS散度而不是Kolmogorov-Smirnov检验或者t检验等传统统计显著性检验方法的原因在于：

样本独立性假设的违背

传统的统计假设检验一般需要比较的两个样本是相互独立的，在这个实验中，抽样子集是总体的一个真子集，即，二者之间有天然的数据重叠以及内在的相关性，不符合两样本假设检验的前提假设，因此不能使用传统的检验方法进行分析。

大样本下的过度敏感性

全集样本容量很大，在这种情况下，传统的统计检验对于很小的变化过于敏感，容易得到统计显著的结果，即，这不能很好地说明这两个样本集在业务逻辑上的一致性。因此相比于二元判断的显著性检验，使用一个有界的并且数值稳定JS散度可以更好地衡量抽样子集对于原始语义空间重构的质量。

本论文使用自助法Bootstrap来检验所得到的结果是否是稳健的。即以从KNN以及MLP模型预测出的所有二级产品类别构成集合为基础，通过上述方法随机抽取30次，然后求每一次所抽到子集与整个集合之间的类内相似性分布的JS散度。

不同的模型预测基础上抽取样本的比较见表3-4。较小的JS散度均值说明在该模型预测结果的基础上，采样算法能更好地保持原始数据的语义分布特征，从而间接反映该模型产出的类别结构具有较好的稳定性。在所有22个一级产品类目里，MLP模型有14个类目的JS散度均值是最小，总体上优于KNN模型。

> **表3-4  基于不同模型预测结果的抽样JS散度均值对比**

基于上述评估结果，本研究最终选择基于MLP模型的预测结果执行抽样，并设定JS散度阈值为0.10，以严格控制子集的分布偏差，采用多随机种子优选策略。对每个一级类别，从[0,100000]范围内随机生成30个候选随机种子，分别抽样并计算抽样分布与原始分布的JS散度，最终选取使JS散度最小化的随机种子。最终的抽样参数及结果详见表3-5，共获得1104条样本组成数据集。在专家完成对数据集的标注工作后，该数据集将用于后续检验自动标注工作流的性能。

> **表3-5  基于MLP的最终抽样结果**

## 自动标注工作流构建

对于近6万条需要标注的数据，人工标注成本高昂并且无法保证一致性，在此本文使用专家子集对大模型标注的工作流进行优化以及校验，从而实现对所有数据自动标注。

### 环境搭建

由于数据量以及成本原因，本文并没有采用API调用方式，在本地搭建了一个完全私有的推理以及调度环境，相关软硬件配置见3-6、表3-7。

推理阶段使用NVIDIA RTX 4060显卡，在此显卡上集成了Ada Lovelace架构，支持FP8以及INT8等低精度运算，在大规模模型量化推理场景下适用，由于Qwen3:8B模型所需显存为8GB，所以此显卡也足够用于本地部署。

在模型推理层面，使用Ollama[64]作为本地大语言模型运行时，在此进行模型加载、量化推理以及服务化部署等工作。这使本文省去许多不必要的工作，节省时间和精力的同时也保证系统的稳定性。

在应用及编排方面，本文使用Dify进行LLM的应用开发以及工作流的实现，同时采用Docker[65]的方式对应用进行容器化部署以便提供一致的操作环境并且便于复制。

> **表3-6  硬件详情**

> **表3-7  软件详情**

### 自动标注工作流设计

为了应对医疗器械专利文本专业性强、类别界限模糊而造成的分类问题，本文利用Dify平台设计一个基于RAG以及LLM的自动标注流程，在本地应用可以通过API调用已经部署好的这个流程。


> **图3-3  自动标注工作流**

工作流使用粗分类加难例精修的方式，利用《医疗器械分类目录》作为外部知识库，在容易混淆类别的基础上增加一个条件分支进行进一步验证，保证标注准确率，工作流的整体结构如图3-3所示，主要包括知识查询、粗分类判断、条件分支校验以及结果汇总等部分。

知识检索模块

本模块用于指导大模型对正确分类。系统接收到一个专利摘要后，使用bge-large-zh-v1.5嵌入模型将其转化为向量表示。然后，在已经建立好的《医疗器械分类目录》知识库中进行混合搜索，语义以及关键词所占比例分别为0.7、0.3，Score阈值为0.4，返回与输入摘要语义最接近的前五个知识片段。这些片段包含了相关类别的定义、核心特征及典型产品示例，为后续的推理过程提供了权威的知识上下文。

初步分类决策模块

这个阶段用较为通用的Qwen3:8B模型做初筛工作，提示词根据角色设定、任务定义、知识注入以及逻辑推理的方式来编写。

角色设定，将模型设定为医疗器械分类专家。

任务定义，完成22个一级产品类别分类。

知识注入，在得到结果之后，将找到的知识片段作为上下文输入模型，并明确告诉模型在做选择时要使用“区分重点”，比如“有源与无源的区别”、“植入与手术器械的区别”。

结构化输出，让模型用JSON形式输出分类结果，“类别”、“理由”以及“决策依据”。

条件分支与二次校验模块

为避免初筛过程中出现误判情况，在工作流中增加条件分支以对某些类目再次甄别。基于前期测试结果，发现四个高频易混类别并分别为其设置专门校验点，如表3-8所示。

> **表3-8  各分支及作用**

变量聚合与输出

变量聚合器汇总各分支最终结果。不管通过何种路径，该系统最终均会得到一个统一格式分类标签以及相应合理解释，对一条专利进行自动标注。

以上述方式，该工作流不仅使用RAG解决大模型所欠缺的知识，还用逻辑分支有效解决医疗器械分类中类之间容易混淆问题，提高自动化标注准确性。

### 自动标注结果评估

为了测试自动标注工作流的效果以及保证最终生成标签的质量，在专家对数据集进行标注后，本文对这个工作流进行了定量化评价，在此期间不断地调整RAG检索的相关参数，优化LLM提示词及LLM参数，增加一些有针对性的条件分支检查来提高模型效果。

由于LLM生成具有一定的随机性，在得到较稳定、可信标注时，本文把LLM温度以及Top-P设为0.5，让LLM回答更保守，从而让其输出更稳定、一致。

最后在数据集上进行10轮自动标注工作流，取这10次的结果中出现次数最多的那个作为最后的结果。

本研究选取了以下三个核心指标对标注质量进行多维度评估：

Accuracy

衡量模型整体分类正确的样本比例，直观反映标注的总体精度。

Macro F1-score

求所有类别的F1分数均值。但由于医疗器械类别分布极不平衡，Macro F1-score更能真实体现模型对于稀有类别性能，而不会被大多数类别所影响。

Fleiss' Kappa系数

用来衡量10次运行的结果是否一致。Kappa值越大，说明模型在不同的轮次所得到的结果越一致，随机性的成分就越小。

> **表3-8  标注效果对比评估**

表3-8给出了原始标签以及经过自动标注流程处理后标签的质量比较情况。由表中可见，原始专利数据的标签质量较差，其准确率为0.5833，无法满足训练高质量分类器的需求；但是经过改进之后的自动标注流程却有着非常大的进步，其准确率为0.9185，并且其Macro F1-score也提升到了0.9109，即在整个类别上以及每个子类别上都能有良好的表现。同时Fleiss' Kappa系数为0.9998，几乎可以认为是一致，这也说明这个流程稳定可靠。

图3-4是工作流混淆矩阵，可清晰看到模型对所有22种医疗器械分类情况。第一，大部分样本集中在矩阵的对角线上，即预测标签与真实标签基本相同。其次，核心类别的识别率较高，在样本数量较多的核心类别中，效果良好。如，“医用成像器械”被正确识别的数量为191个，“医用诊察和监护器械”为180个，“无源手术器械”为115个。最后，误判率很低，在非对角线位置出现的概率较小并且都很小，说明错分的情况较少。只是在一些语义或者功能非常相似的类之间有一些误判，比如“有源手术器械”有5个被错误地分类成“无源手术器械”，这是在细粒度分类中常见的问题，但是并不会对整个实验的结果产生很大影响。


> **图3-4  工作流的混淆矩阵**

混淆矩阵可视化结果亦与前面提到的0.9185 Accuracy以及0.9109 Macro F1-score相吻合，说明这种自动标注流水线具有良好的鲁棒性和稳定性。

## 数据增强

在使用自动标注的工作流为剩下数据打上标签之后，本研究使用数据集对分类器进行训练，在实验结果中，“输血、透析和体外循环器械”类别的F1-score在所有的分类器中都比随机猜测的baseline更低，说明这个分类器对于这个类别的识别效果不好。而且这个类别在数据集中的数量最少，只有46个，严重不平衡。

为了解决上述问题，本文采用大语言模型实现生成式数据增强。即从该类别的已有的专利中提取其独立权利要求以及第一权利要求作为提示词让大语言模型根据其中的技术内容，在保留主要意思的基础上对其进行改写从而得到类似专利摘要形式的文章。一共生成合成数据92篇，最后加入的数据集共有58587篇，见表3-1。

